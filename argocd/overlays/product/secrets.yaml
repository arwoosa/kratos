apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: identity-secrets
spec:
  data:
  - secretKey: DNS
    remoteRef:
      key: /product/ENV
      property: Identify_DNS
  - secretKey: identity.json
    remoteRef:
      key: /product/Identity.json